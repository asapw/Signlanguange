<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@drawable/bg1"> <!-- Set the background image here -->

    <ImageButton
        android:id="@+id/startButton"
        android:layout_width="265dp"
        android:layout_height="70dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="420dp"
        android:background="@android:color/transparent"
        android:contentDescription="Start Button"
        android:scaleType="fitCenter"
        android:src="@drawable/btnstart" />

    <ImageButton
        android:id="@+id/dicButton"
        android:layout_width="250dp"
        android:layout_height="70dp"
        android:layout_below="@id/startButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="5dp"
        android:background="@android:color/transparent"
        android:contentDescription="Dic Button"
        android:scaleType="fitCenter"
        android:src="@drawable/btndic" />

</RelativeLayout>
